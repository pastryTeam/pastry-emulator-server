// Generated by CoffeeScript 1.9.3
var ArgumentParser, args, i, j, parser, pkgObj, ref;

pkgObj = require('../package.json');

ArgumentParser = require('argparse').ArgumentParser;

parser = new ArgumentParser({
  version: pkgObj.version,
  addHelp: true,
  description: 'Pastry模拟器服务器',
  prog: 'emulator-server'
});

args = [
  ['mode'], {
    help: '标记使用本地模拟数据 或者 远程前置数据',
    choices: ['server', 'local'],
    defaultValue: 'server'
  }, ['-a', '--addr'], {
    help: '指定监听IP',
    defaultValue: '127.0.0.1'
  }, ['-p1', '--port1'], {
    help: '指定监听端口。',
    defaultValue: 7777
  }, ['-s', '--server'], {
    help: '指定前置服务器。',
    // defaultValue: '127.0.0.1'
    // defaultValue: '123.57.85.171'
    defaultValue: 'icitic-cd.eicp.net'
  }, ['-p2', '--port2'], {
    help: '指定前置服务器端口号',
    // defaultValue: '11111'
    // defaultValue: '8080'
    defaultValue: '11884'
  }, ['-app', '--appName'],{
    help: '前置名称',
    // defaultValue: 'CDFO'
    // defaultValue: 'fo-demo'
    defaultValue: 'fo_mobile_loan'
  },['-w', '--workdir'], {
    help: '指定工作目录。',
    defaultValue: '.'
  }, ['--verbose'], {
    help: '设置是否打印详细日志。',
    defaultValue: true
  }
];

for (i = j = 0, ref = args.length / 2; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
  parser.addArgument(args[i * 2], args[i * 2 + 1]);
}

module.exports = parser;
